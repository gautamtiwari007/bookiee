import React from "react";
import classes from "./Sell.module.css";

const Sell = () => {
    return (
        <React.Fragment>
            <a className={classes.sell} href="#">Sell</a>
        </React.Fragment>
    );
};

export default Sell;