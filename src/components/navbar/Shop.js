import React from "react";
import classes from "./Shop.module.css";

const Shop = () => {
    return (
        <React.Fragment>
            <a className={classes.shop} href="#">Shop</a>
        </React.Fragment>
    );
};

export default Shop;